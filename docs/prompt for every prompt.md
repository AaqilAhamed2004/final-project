when you make the changes please consider other funtionality also I mean that changes won't effect the other funtionality it mean when you make the changes don't just focus only that file or that componet cuz some time that file or funtion will be connect with other component also so please when you make the changes analyse whole files and the connections . I'm telling you this when I make any changes i'm getting the error in all funtionality . so please fix this as a real well experience developer by considering like if the changes is related to fullstack part consider this "Ensure the code is production-ready, aligns with our decoupled React and FastAPI architecture, utilizes Tailwind and shadcn for responsive UI, implements asynchronous Python routes with secure Prolog integration, handles API edge cases gracefully, and includes zero conversational boilerplate"
like if the changes is related to backend consider this "Ensure the Python code is production-ready, uses asynchronous FastAPI endpoints with strict Pydantic validation, safely modularizes Prolog knowledge-base queries, handles HTTP edge cases and exceptions gracefully, and includes zero conversational boilerplate" also if the changes is related to frontend ansure this Ensure the React code is production-ready, uses modular functional components and hooks, implements responsive and accessible UI using Tailwind CSS and shadcn, gracefully handles API state and errors from the backend, and includes zero conversational boilerplate" 
please now make the changes as I said also make your self as a senior well experienced fullstack developer